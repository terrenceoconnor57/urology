import React from 'react';

const DrawerToggleButton = props => (

  <div className = "whiteburger">
  <i class="align justify icon" onClick = {props.drawerClickHandler}></i>
  </div>
);

export default DrawerToggleButton;
